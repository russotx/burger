<div class="third">
  <h3>Fresh Burgers</h3>
    {{#each viewArr}}
      {{#unless this.devoured}}
        <div class="burger fresh">{{this.name}}<button id="{{this.name}}" class="devour-btn"></button></div>
      {{/unless}}
    {{/each}}
</div>

<div class='third'>

  <img src="assets/img/burger.jpg" alt="burger">

</div>
  
<div clas='third'>
  <h3>Devoured Burgers</h3>
  {{#each viewArr}}
      {{#if this.devoured}}
        <div class="burger dev">{{this.name}}</div>
      {{/if}}
  {{/each}}
</div>